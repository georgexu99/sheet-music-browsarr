# Pinned to the versions the harvest technique was proven against on the
# desktop (see HANDOFF.md). nodriver 0.50.3 specifically: its
# cdp.network.get_response_body() is broken, which is *why* the worker
# screenshots pages instead of capturing bytes — do not bump blindly.
nodriver==0.50.3
aiohttp==3.14.1
img2pdf==0.6.3
